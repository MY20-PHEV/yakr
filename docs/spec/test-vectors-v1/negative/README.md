# Negative vectors — `yakr-v1.0`

Each entry defines a **protocol-level rejection**. See [negative-vector-outcomes-v1.md](../../negative-vector-outcomes-v1.md) for normative error codes and stages.

Required fields: `must_reject`, `rejection_stage`, `normative_error_code`, `persistent_state_must_change`, `retryable`.

`error_contains` is optional (reference verifier hint only).
